# Copy to .env.local and set for production deployment
# In development, the Vite proxy handles /api → localhost:8000 automatically

# Your deployed FastAPI backend URL (Render, Railway, etc.)
# Leave empty for local dev
VITE_API_URL=
